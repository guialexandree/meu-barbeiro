import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#3399ff',
    },
    secondary: {
      main: '#ffa726',
    },
    background: {
      default: '#010205',
      paper: '#151515',
    },
  },
})

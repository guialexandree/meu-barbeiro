version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: .docker/Dockerfile
    container_name: meu-barbeiro
    ports:
      - "8080:8080"
    env_file:
      - /home/meu-barbeiro/.env
    restart: always